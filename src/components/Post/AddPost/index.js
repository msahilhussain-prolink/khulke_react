import React from "react";

import { Avatar, Input } from "./style";

const AddPostDialog = () => {
  return (
    <div>
      <Avatar src={""} />
      <Input placeholder="Write something..." />
    </div>
  );
};

export default AddPostDialog;
